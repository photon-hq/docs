---
title: "Catalog"
description: "Every RFC 9457 problem type the Photon API publishes."
mode: "wide"
---

import { ProblemsTable } from "/snippets/problems-table.jsx"

Every problem type the API publishes, generated from the staging OpenAPI schema.
See [Handling errors](/problems/handling-errors) for what each field means.

A few services still emit a legacy `urn:photon:problem:` type URI; those are not
listed here.
